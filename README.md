# mindmap

シンプルなマインドマップエディタ。

公開ページ: https://octop162.github.io/mindmap/

## 注意

複数タブ・複数ウィンドウでの同時利用は非対応。自動保存は `localStorage` を全タブで共有しているため、後から書き込んだタブの内容で他タブの編集が上書きされて失われる(編集していない覗き見タブを閉じただけでも巻き戻ることがある)。編集は 1 タブのみで行うこと。
